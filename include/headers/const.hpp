#ifndef CONST_HPP
#define CONST_HPP

static const int scale = 7;
static const int visWidth = 100 * scale;
static const int sidebarWidth = 40 * scale;
static const int screenWidth = visWidth + sidebarWidth;
static const int screenHeight = 100 * scale;

#endif